/**
 * 'matrices' must contain '.expr' elements which contain two matrices in tables
 * Returns the element which will contain the interactive exercise.
 */
export function createFrom(matrices) {
    let interact_container = document.createElement("div");
    return interact_container;
}
